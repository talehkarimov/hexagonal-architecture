spring:
  application:
    name: hexagonal-architecture

  datasource:
    url: jdbc:mysql://localhost:3306/hexagonal_architecture
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080